import "./Gallery.scss";

const Gallery = () => {
    return (
        <section className="gallery">
            <h2 className="title">Галерея</h2>
        </section>
    );
};

export default Gallery;
